enum PrototypeMode {
    case chord
    case strum
}

enum PrototypeScreen {
    case main
    case strumSelect
    case chordProgression
}

enum PrototypePeerButtonState {
    case disconnected
    case connecting
    case connected
}
